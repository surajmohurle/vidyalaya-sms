version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - SQLALCHEMY_DATABASE_URI=sqlite:///./vidyalaya.db
    restart: always

# If switching to Postgres in the future, add the DB service here
